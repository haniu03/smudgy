{
    "version": "0.0.1",
    "description": "Type TinTin++ commands at the input line: #alias, #action, #variable, #showme and friends become native smudgy automations.",
    "entry": "index.ts",
    "min_smudgy_version": "0.5.0",
    "options": [
        {
            "key": "commandChar",
            "label": "Command character",
            "type": "string",
            "default": "#"
        }
    ],
    "permissions": {
        "read": ["$DATA"],
        "write": ["$DATA"],
        "smudgy": {
            "input": ["access"],
            "interop": ["read"],
            "automations": ["aliases", "triggers"],
            "session": ["send", "echo", "reach-others"],
            "display": ["change"],
            "panes": ["create"]
        }
    }
}
